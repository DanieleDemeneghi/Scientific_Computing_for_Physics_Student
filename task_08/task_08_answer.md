# Task_08 answers

## Check that d is the same as the original code and check that this last sum is the same as the sum of all the elements of d in the original code

For every i ```d[i] == d_chuncked[i]```, while ```sum == sum_chunk``` holds only for ``` tolerance > 1.0E-11``` .